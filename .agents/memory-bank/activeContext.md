# Active context

**Updated:** 2026-06-18

## Current focus

**M-16 — MCP self-description protocol** (draft). Make MCP tools discoverable by generic chat clients via `serverInstructions` and prompt argument exposure.

## In progress

- [x] M1–M9, M-11–M-14 complete
- [ ] M-16: MCP server `serverInstructions`, client-side capture + catalog

## Next steps

1. M-16 task 1: set `serverInstructions` in `ai-architect-6-mcp`
2. M-16 tasks 2–5: ai-chat side (McpServerInfo, connector, registry catalog)
3. End-to-end verification
