# M-18 — Multi-agent memory-bank migration

**Status:** Done
**Date completed:** 2026-06-21
**Branch:** develop
**Decision:** DEC-014

## What changed

Migrated the legacy flat `.agents/memory-bank/` (`activeContext.md`, `progress.md`, `decisions.md` rewritten/appended each session) into the multi-agent-safe canonical structure:

- `registry/*.jsonl` — append-only ID registries (req, nfr, scn, test, dec, risk, task)
- `records/progress/M{NN}.md` — one file per completed milestone (M01–M17)
- `records/active/M15.md` — active milestone record (replaces `activeContext.md`)
- `records/decisions/DEC-###.md` — long-form decision bodies
- `locks/` — module ownership claims for coupled-file edits
- `worktrees/` — git-ignored per-branch scratchpads
- `scripts/sync-memory-index.sh` — regenerates generated index files; `--check` for CI
- Root `AGENTS.md` rewritten compact + multi-agent memory section
- `docs/ai-context-strategy.md` rewritten for new structure

## Affected IDs

- Decision: DEC-014
- Legacy DEC-001..DEC-013 migrated to `dec.jsonl` + `records/decisions/`

## Modules touched

- `all` (context layer only; no app code changed)

## Verification

- `scripts/sync-memory-index.sh` regenerates `activeContext.md`, `progress.md`, `decisions.md`, `productContext.md` tables, `plans/00-index.md`
- `scripts/sync-memory-index.sh --check` exits 0